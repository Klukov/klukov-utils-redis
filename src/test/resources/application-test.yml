spring:
  data:
    redis:
      host: localhost
      port: 6379

klukov:
  utils:
    redis:
      limiter:
        limit-duration: P1D
        max-confirmation-duration: PT15M
        redis-key-prefix: test-klukov-utils-redis-limiter
      lock:
        simple:
          redis-prefix: test-klukov-utils-redis-lock-simple
